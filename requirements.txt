# Core dependencies
pyyaml>=6.0

# PySpark — only needed for local development/testing
# On Databricks, PySpark is pre-installed on the cluster
pyspark>=3.5.0

# Testing
pytest>=7.0
pytest-cov>=4.0

# Linting & formatting
ruff>=0.4.0

# Build
build>=1.0
setuptools>=68.0
wheel
